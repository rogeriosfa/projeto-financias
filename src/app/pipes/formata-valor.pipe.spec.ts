import { FormataValorPipe } from './formata-valor.pipe';

describe('FormataValorPipe', () => {
  it('create an instance', () => {
    const pipe = new FormataValorPipe();
    expect(pipe).toBeTruthy();
  });
});
