import { SaldoColorDirective } from './saldo-color.directive';

describe('SaldoColorDirective', () => {
  it('should create an instance', () => {
    const directive = new SaldoColorDirective();
    expect(directive).toBeTruthy();
  });
});
